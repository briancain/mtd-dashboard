require 'spec_helper'

describe "Login Auth" do
  pending "This does not pass on travis, but works locally. #{__FILE__}"
  # page_base_title = 'MTD Dashboard'

  # before :each do
  #   User.create(:email => 'derp@herp.edu', :password => 'password')
  # end

  # it "signs me in" do
  #   visit '/dashboard/home'
  #   within("#session") do
  #     fill_in 'Login', :with => 'bccain@ksu.edu'
  #     fill_in 'Password', :with => 'password'
  #   end
  #   click_button 'Sign in'
  #   # puts "#{page.html.inspect}"
  #   expect(page).to have_content 'Welcome'
  # end
end
