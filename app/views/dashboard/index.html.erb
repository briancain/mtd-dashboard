<% provide(:title, 'Manage') %>
<head>
  <style type="text/css">
    body {
      padding-top: 50px;
    }
    .starter-template {
      padding: 40px 15px;
      text-align: center;
    }

    #customli {
        max-width: 50%;
    }

    #editor {
        // position: absolute;
        width: 500px;
        height: 400px;
    }
  </style>
</head>
<div class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">MTD Dashboard</a>
    </div>
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
        <li><a href="/">Home</a></li>
        <% if user_signed_in? %>
          <li class="active"><a href="/dashboard/index">Manage</a></li>
        <% end %>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= current_user.name %><b class="caret"></b></a>
            <ul class="dropdown-menu">
              <li><%= link_to("Edit Profile", edit_user_registration_path) %></li>
              <li><%= link_to("Hack the Planet", "/dashboard/index") %></li>
            </ul>
          </li>
          <form class="navbar-form navbar-right">
            <div><a class="btn btn-danger" <%= link_to('Logout', destroy_user_session_path, :method=>'delete')%></a></div>
          </form>
        <% else %>
          <form class="navbar-form navbar-right">
            <div><a class="btn btn-primary" href=<%= new_user_registration_path %>>Sign up</a></div>
          </form>
          <form class="navbar-form navbar-right">
            <div><a class="btn btn-success" href=<%= new_user_session_path %>>Login</a></div>
          </form>
        <% end %>
      </ul>
    </div><!--/.nav-collapse -->
  </div>
</div>

<div class="container">
  <h1 align="center">AngularJS Element Demo</h1>
  <center><button type="submit" class="btn btn-info" onclick="aboutAngular()">About</button><br/><br/></center></center>
  <center>
    <div ng-controller="DashboardCtrl">
      <form class="form-inline" role="form" ng-submit="addEntry()">
        <div class="form-group">
          <input type="text" ng-model="newEntry.name" class="form-control" placeholder="Entry goes here">
        </div>
        <input type="submit" value="Add" class="btn btn-default">
      </form>

      <center>
        <ul class="list-group" id="customli">
          <li class="list-group-item" ng-repeat="entry in entries">
            {{entry.name}}
          </li>
        </ul>
      </center>
    <div>
  </center>

  <center><h1>Editor Config Demo</h1>
    <button type="submit" class="btn btn-info" onclick="aboutEditor()">About</button><br/><br/></center>
  <div class="row">
    <div class="col-md-6">
      <div id="editor"></div>
    </div>
    <div class="col-md-6">
      <div class="btn-group">
        <button type="button" class="btn btn-success dropdown-toggle" data-toggle="dropdown">
          Load Preset <span class="caret"></span>
        </button>
        <ul class="dropdown-menu" role="menu">
          <li><a href="#" onclick="insertData('goal')"><b>goal</b></a></li>
          <li class="divider"></li>
          <li><a href="#" onclick="insertData('ecommerce')">- exampleGoal</a></li>
          <li><a href="#" onclick="insertData('role')"><b>role</b></a></li>
          <li class="divider"></li>
          <li><a href="#" onclick="insertData('exampleRole')">- exampleRole</a></li>
          <li><a href="#" onclick="insertData('cluster')"><b>cluster</b></a></li>
          <li class="divider"></li>
          <li><a href="#" onclick="insertData('exampleCluster')">- exampleCluster</a></li>
        </ul>
      </div>
    </div>
  </div><br/>

  <div class="row">
    <div class="col-md-6">
      <div class="btn-group">
        <button type="submit" class="btn btn-default" >Open</button>
        <button type="submit" class="btn btn-default" onclick="displayInfo()">Save</button>
        <button type="submit" class="btn btn-default" onclick="doUndo()">Undo</button>
        <button type="submit" class="btn btn-default" onclick="doRedo()">Redo</button><br/><br/>
      </div>
    </div>
  </div>

  <script src="//code.jquery.com/jquery.js"></script>
  <script src="/js/ace.js"></script>
  <script src="/js/mode-yaml.js"></script>
  <script>
    var editor = ace.edit("editor"),
        YAMLMode = require("ace/mode/yaml").Mode;

    editor.getSession().setMode(new YAMLMode());
    editor.getSession().setTabSize(2);
    editor.getSession().setUseWrapMode(true);
    editor.getSession().setUseSoftTabs(true);

    var undoRedoManager = editor.getSession().getUndoManager();

    $.get('/tmp/fullstack.yaml', function(data) {
        editor.insert(data);
    });

    $(document).ready(function() {
      move();
    });

    function move() {
      var editor = ace.edit("editor");

      editor.gotoLine(0);
    }

    function insertData(data) {
      var msg = "";
      if (data == 'goal') {
        msg = "goals:\n\tecommerce:\n\t\tname: TemplateName\n\troles:\n\t\t\t- TemplateRole";
      }
      else if (data == 'role') {
        msg = "roles:\n\texampleRole:\n\t\tname: TemplateName\n\t\tmin: 1\n\t\texports:\n\t\t\thttp: { type: exampleType, protocol: exampleProtocol }\n\t\t\thttps: { type: exampleType, protocol: exampleProtocol }\n\t\timports:\n\t\t\t- exampleImport";
      }
      else if (data == 'cluster') {
        msg = "clusters:\n\texampleCluster:\n\t\tname: ExampleName\n\t\tstrategy: ExampleStrategy\n\t\troles:\n\t\t\t- exampleRole";
      }

      editor.insert(msg);
    }

    function doUndo() {
      if (undoRedoManager.hasUndo()) {
        undoRedoManager.undo();
      }
    }

    function doRedo() {
      if (undoRedoManager.hasRedo()) {
        undoRedoManager.redo();
      }
    }

    function displayInfo() {
      var editor = ace.edit("editor");
          data = editor.getSession().getValue();

      alert(data);
    }

    function aboutEditor() {
      var msg = "This editor shows how a user can edit our DSL to generate a configuration";

      alert(msg);
    }

    function aboutAngular() {
      var msg = "This demo shows off AngularJS. It has a controller where it stores the 3 default values you see, and uses the view to retrieve and display them.";

      alert(msg)
    }
  </script>
</div>
